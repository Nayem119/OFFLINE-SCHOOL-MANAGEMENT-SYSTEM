# Release Notes — 2.0.0

This release packages the Offline School Management System as a Windows desktop
application with Start Menu and optional desktop shortcuts.

The uninstaller removes installed application files and shortcuts only. The
database, backups, configuration, and student photos remain in
`%LOCALAPPDATA%\Offline School Management System` so important school data is
not accidentally deleted.
